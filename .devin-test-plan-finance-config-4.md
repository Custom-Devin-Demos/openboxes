# Test plan — PR #27 finance-config-4 (paymentTerm list, preferenceType list/create/edit → React)

App: http://localhost:8080/openboxes, admin/password. DB: docker openboxes-db (mariadb).
Branch: devin/1787297097-finance-config-4. JAVA_HOME=/usr/lib/jvm/java-8-openjdk-amd64.

## 1. BEFORE (develop, legacy GSP)
- Boot app on develop, reseed demo data (docker exec -i openboxes-db mysql -uopenboxes -popenboxes openboxes < docker/seed-demo-data.sql).
- Run branch's shot.js (saved at /tmp/shot-branch.js) → /tmp/shots/before.
- Record: paymentTerm list row count + columns; preferenceType list rows (expect 6 install-seeded types) + columns; create/edit form fields.
- PASS: screenshots show legacy GSP tables with data.

## 2. AFTER (branch, React) — recorded browser session
- Checkout branch, restart app, reseed if needed.
- Test A: /paymentTerm/list renders React DataTable with the SAME payment terms as BEFORE (e.g. PP100, NET30…), same column set (name, code, days, etc.). FAIL if empty table, error toast, or legacy GSP renders.
- Test B: /preferenceType/list shows the same 6 preference types as BEFORE with Create button (admin).
- Test C (CRUD e2e): Click Create → fill Name "Test Pref QA" + pick a Validation Code option → Create → PASS iff success notification and redirect to /preferenceType/edit/<newId> with fields populated. Change name to "Test Pref QA2" → Update → PASS iff redirect to list showing 7 rows incl. "Test Pref QA2". Open its edit → Delete → confirm Yes → PASS iff redirect to list with 6 rows, row gone.
- Capture AFTER screenshots via shot.js → /tmp/shots/after; compare data parity with BEFORE.

## 3. Suites (branch, shell)
- Playwright UI characterization: cd e2e && npx playwright test → PASS iff all pass (expected 10/10). Output → /tmp/ui-suite.log.
- API snapshots: reseed DB fresh, cd api-snapshots && npm run snapshots:verify. Expect only localizations-list to fail (new react.* keys). Re-record only that snapshot additively (verify diff adds keys, removes none), leave file modified, re-run verify → PASS iff 122/122. Outputs → /tmp/api-verify-1.log, /tmp/api-verify-2.log.

## Pass criteria summary
- Data parity before/after on both list screens; CRUD works end-to-end via UI; UI suite green; API suite 122/122 after additive localizations re-record only.
