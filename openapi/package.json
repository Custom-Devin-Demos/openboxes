{
  "name": "openboxes-openapi-contracts",
  "version": "1.0.0",
  "private": true,
  "description": "OpenAPI specification and contract tests for the OpenBoxes REST API. Requires Node >= 18 (Node 20 recommended, matching CI).",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "contracts:bundle": "node src/bundle.js",
    "contracts:lint": "node src/bundle.js && redocly lint dist/openbox.bundled.json --config redocly.yaml",
    "contracts:verify": "node src/verify.js"
  },
  "dependencies": {
    "ajv": "8.17.1",
    "ajv-formats": "3.0.1",
    "js-yaml": "4.1.0"
  },
  "devDependencies": {
    "@redocly/cli": "1.34.3"
  }
}
